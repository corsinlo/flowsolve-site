/// <reference types="astro/client" />

interface ImportMetaEnv {
  readonly PUBLIC_PREVIEW_MODE?: string;
  readonly PUBLIC_PILOT_REQUEST_URL?: string;
  readonly PUBLIC_PILOT_SIGN_IN_URL?: string;
}
